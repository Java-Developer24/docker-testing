name: performance-arena-frontend
location: eastus

properties:
  configuration:
    ingress:
      external: true
      targetPort: 80
  template:
    containers:
      - name: frontend
        image: <ACR_NAME>.azurecr.io/frontend:latest
